# AIO 说明文档

## 1. 概述

AIO（Asynchronous I/O）是异步输入输出模型，核心目标是在等待 I/O 操作时不阻塞线程，以更少线程处理更多并发请求。它特别适合网络通信、文件读写等 I/O 密集型场景。

## 2. 适用场景

- 高并发接口服务
- 长连接通信（如聊天、推送）
- 批量远程调用或爬取任务
- 文件上传下载与流式处理

## 3. 核心概念

- **事件循环（Event Loop）**：负责调度和分发异步任务
- **协程（Coroutine）**：可暂停、可恢复的执行单元
- **任务（Task）**：协程的可管理运行实例
- **等待（await）**：等待异步结果，不阻塞主流程
- **取消与超时（Cancel/Timeout）**：控制任务生命周期，避免无限等待

## 4. 基本流程

1. 创建事件循环
2. 定义异步任务
3. 将任务提交到事件循环
4. 在任务完成后收集结果
5. 统一处理异常、超时和资源释放

## 5. 最佳实践

- 优先使用异步客户端（HTTP、数据库、消息队列），确需阻塞代码时放入线程池隔离。
- 为每一层调用设置超时（连接、读写、总超时），并区分可重试与不可重试错误。
- 用信号量、连接池和队列长度限制并发，避免瞬时流量把下游压垮。
- 对瞬时失败采用指数退避重试，达到阈值后快速失败或降级，防止雪崩放大。
- 统一记录请求 ID、耗时、重试次数和异常原因，保证异步链路可观测、可追踪。

## 6. 常见误区

- 误区 1：异步一定更快  
  说明：仅在 I/O 密集场景通常收益明显，CPU 密集任务应使用多进程或线程池。

- 误区 2：协程可以替代一切并发模型  
  说明：协程不是万能方案，应按业务特点组合使用。

## 7. 总结

AIO通过非阻塞方式提升并发处理能力，能显著优化 I/O 密集型系统的吞吐和响应。合理设计超时、并发上限和异常处理，是稳定落地 AIO 的关键。
